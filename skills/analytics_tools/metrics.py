from __future__ import annotations

from typing import Any


def simple_funnel_stub() -> dict[str, Any]:
    return {
        "visitors": 1000,
        "signup_rate": 0.04,
        "activation_rate": 0.35,
        "note": "Replace with warehouse / product analytics connector.",
    }
