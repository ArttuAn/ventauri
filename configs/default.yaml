default_pipeline: idea-to-strategy

product:
  name: Ventauri
  cli_command: ventauri

orchestrator:
  max_retries: 2
  priority: balanced

memory:
  vector_dim: 96

api:
  host: "0.0.0.0"
  port: 8000
